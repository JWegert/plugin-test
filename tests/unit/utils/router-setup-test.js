import routerSetup from 'dummy/utils/router-setup';
import { module, test } from 'qunit';

module('Unit | Utility | router setup');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = routerSetup();
  assert.ok(result);
});
